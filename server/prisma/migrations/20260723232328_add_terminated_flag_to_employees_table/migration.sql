BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Employees] ADD [terminated] BIT NOT NULL CONSTRAINT [Employees_terminated_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
