BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[WorkOrders] ADD [due_date] DATETIME2 NOT NULL CONSTRAINT [WorkOrders_due_date_df] DEFAULT CURRENT_TIMESTAMP,
[start_date] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
